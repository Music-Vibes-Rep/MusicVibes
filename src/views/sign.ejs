<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= isRegister ? 'Registro' : 'Login' %> - MusicVibe</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gray-50">
    <div class="min-h-screen flex items-center justify-center">
        <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-md">
            <!-- Encabezado dinámico -->
            <h2 class="text-2xl font-bold text-gray-900 mb-6 text-center">
                <%= isRegister ? 'Crear Cuenta' : 'Iniciar Sesión' %>
            </h2>

            <!-- Formulario dinámico -->
            <form action="<%= isRegister ? '/registro' : '/login' %>" method="POST">
                <% if(isRegister) { %>
                    <div class="mb-4">
                        <label class="block text-sm font-medium text-gray-700 mb-1" for="nombre">Nombre</label>
                        <input type="text" id="nombre" name="nombre" required
                            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500">
                    </div>
                <% } %>

                <div class="mb-4">
                    <label class="block text-sm font-medium text-gray-700 mb-1" for="email">Correo electrónico</label>
                    <input type="email" id="email" name="email" required
                        class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500">
                </div>

                <div class="mb-6">
                    <label class="block text-sm font-medium text-gray-700 mb-1" for="password">
                        Contraseña
                        <% if(isRegister) { %>
                            <span class="text-gray-400">(mínimo 8 caracteres)</span>
                        <% } %>
                    </label>
                    <input type="password" id="password" name="password" required
                        <%= isRegister ? 'minlength="8"' : '' %>
                        class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500">
                </div>

                <button type="submit"
                    class="w-full bg-indigo-600 text-white py-2 px-4 rounded-md hover:bg-indigo-500 transition duration-200">
                    <%= isRegister ? 'Registrarse' : 'Ingresar' %>
                </button>
            </form>

            <!-- Switch entre formularios -->
            <div class="mt-4 text-center">
                <% if(isRegister) { %>
                    <p class="text-sm text-gray-600">¿Ya tienes cuenta?
                        <a href="/login" class="text-indigo-600 hover:text-indigo-500">Inicia sesión</a>
                    </p>
                <% } else { %>
                    <p class="text-sm text-gray-600">¿No tienes cuenta?
                        <a href="/registro" class="text-indigo-600 hover:text-indigo-500">Regístrate</a>
                    </p>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
