<!DOCTYPE html>
<html lang="es" class="transition-colors duration-300 dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Publicar - MusicVibe</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #fda4af, #ffffff, #fbcfe8);
    }
    .dark body {
      background: linear-gradient(135deg, #0f172a, #1e293b);
    }
  </style>
</head>

<body class="min-h-screen flex items-center justify-center px-4 text-gray-900 dark:text-gray-100">

  <div class="bg-white dark:bg-gray-900 rounded-3xl shadow-2xl w-full max-w-lg p-10 mt-20">
    <h2 class="text-4xl font-extrabold text-center text-pink-700 dark:text-pink-400 mb-8">Crear publicación</h2>

    <form method="POST" action="/publicar" enctype="multipart/form-data" class="space-y-6" onsubmit="return validarFormulario();">
      
      <div>
        <label for="contenido" class="block text-lg font-medium mb-2">¿Qué quieres compartir hoy?</label>
        <textarea id="contenido" name="contenido" placeholder="Escribe aquí tu publicación..." rows="5"
          class="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-700 focus:outline-none focus:ring-2 focus:ring-pink-500 dark:bg-gray-800 resize-none"
          required></textarea>
      </div>

      <div>
        <label for="imagen" class="block text-lg font-medium mb-2">Subir imagen (opcional)</label>
        <input id="imagen" name="imagen" type="file" accept="image/*"
          class="w-full px-3 py-2 border border-gray-300 dark:border-gray-700 rounded-lg bg-white dark:bg-gray-800 text-gray-700 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-pink-500" />
      </div>

      <div class="flex flex-col sm:flex-row sm:space-x-4 space-y-4 sm:space-y-0">
        <button type="submit"
          class="w-full bg-pink-600 hover:bg-pink-700 text-white font-semibold py-3 rounded-lg shadow-md transition-all">
          Publicar
        </button>
        <button type="reset"
          class="w-full bg-gray-300 hover:bg-gray-400 text-gray-800 font-semibold py-3 rounded-lg shadow-md transition-all">
          Cancelar
        </button>
      </div>

    </form>
  </div>
</body>
</html>
