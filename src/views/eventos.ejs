<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Eventos Musicales | MusicVibe</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-r from-red-600 to-pink-700 text-white min-h-screen">
  <%- include('partials/_navbar') %>

  <main class="container mx-auto py-10 px-4">
    <h1 class="text-3xl font-bold text-white mb-6">🎤 Eventos musicales en tu zona</h1>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% eventos.forEach(e => { %>
        <div class="bg-white text-gray-900 rounded-xl shadow p-4 space-y-2">
          <% if (e.imagen) { %>
            <img src="<%= e.imagen %>" alt="Evento" class="rounded-md w-full h-48 object-cover">
          <% } %>
          <h2 class="text-lg font-bold text-red-600"><%= e.titulo %></h2>
          <p class="text-sm text-gray-700"><%= e.descripcion %></p>
          <p class="text-sm text-gray-500">📍 <%= e.lugar %></p>
          <p class="text-sm text-gray-500">📅 <%= new Date(e.fecha).toLocaleDateString('es-ES') %></p>
          <a href="<%= e.url %>" target="_blank" class="inline-block mt-2 text-sm font-semibold text-pink-600 hover:underline">
            Ver en <%= e.fuente %>
          </a>
        </div>
      <% }) %>
    </div>
  </main>

  <%- include('partials/_footer') %>
</body>
</html>
